# ── Motor de decisión FPL (mova_fpl/) ──────────────────────────────────────
pandas>=2.3.0
numpy>=2.0.0
scipy>=1.16.0
scikit-learn>=1.7.0        # modelo de minutos (clasificación calibrada)
pulp>=3.3.0                # MILP del optimizador (solver CBC incluido)
joblib>=1.5.0              # serialización de artefactos de modelo

# ── Pruebas ────────────────────────────────────────────────────────────────
pytest>=8.0.0

# ── Legacy: colectores del Mundial 2026 y apuestas (capítulo cerrado) ──────
cloudscraper>=1.2.71
