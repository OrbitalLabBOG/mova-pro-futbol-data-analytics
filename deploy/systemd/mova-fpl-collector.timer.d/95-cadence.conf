# Evaluate source cadences every 15 minutes. Admission remains non-blocking;
# a busy capacity lock must not defer the next opportunity for a full hour.
[Timer]
OnCalendar=
OnCalendar=*:0/15
Persistent=true
RandomizedDelaySec=45
AccuracySec=15s
